# Runtime execution acceptance evidence

A successful CPU runtime execution must retain evidence for the exact runtime and model inputs, native extension import, CPU platform selection, generated tokens, dependency inventory, and proof digest. The workflow must fail closed and must not mark the broader production runtime certified.
