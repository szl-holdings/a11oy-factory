# Assurance levels

- `runtime_execution_verified=true` means the named, pinned runtime generated tokens from the named, pinned model revision on the named target during the recorded workflow run.
- `production_runtime_certified=false` remains mandatory until wider compatibility, performance, security, resilience, and operational evidence is admitted.
