# PR scope

This change adds one executable, evidence-producing CPU AMD64 vLLM smoke path. Pull-request checks validate the contract; the actual model execution runs only after merge to main or explicit workflow dispatch.
