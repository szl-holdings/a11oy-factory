# Executable target

The CPU AMD64 smoke path installs only factory-qualified wheel inputs, verifies a pinned Hugging Face model revision and model-file digest, imports the native vLLM extension, requires CPU platform selection, generates tokens, and emits a canonical scoped proof.
